import 'package:flutter/material.dart';

class MyCalls extends StatelessWidget {
  const MyCalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('C A L L S'),
    );
  }
}
